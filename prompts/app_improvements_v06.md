- at the dropdown that display every task, like at execute view and evaluate metrics view, the tasks should collapse under task and recurrent task label, and should collapse recurrent groups too
- when moving a task around on the plan view left panel, the position where it would land if dropped is marked with a light blue border. I would like that if I just want to move the task in the middle of to existing tasks (not as a subtask), to be marked as a light blue line over the border that the other two tasks share (the ones that would end up being the dragged task neighbour if dropped).
- can you add a configuration button at the top left? when click I would like to see a dialog with configuration options. For now it is empty
- at the configuration dialog can you add an option to track keyboard an mouse movement when time tracking? then, if no activity has been detected on a configurable amount of time the time tracker stops (without marking the tracked task as done). can you also add a sound for this event so if the user is there realizes the time tacker was stopped?
- Can you modify the chrome tab logo for this app? It must be green and show the timer when a task is beign tracked and red if the tacker has been stopped by no interaction.