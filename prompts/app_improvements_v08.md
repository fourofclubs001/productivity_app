Lets make this app a real web app for many users
can we use firebase for this?
I would like the user to be able to register only with their google account or with a verification code to its email.
On the visual and UX it still feels a bit rusty, can you launch a fable agent for writting down suggestion for then a sonnet 5 to implement them (after approval)

- the tab title when the timer is tracking shows the tracked time. If the timer exceed the hour, it tell the amount of time in minutes. I would like it to show mm:ss before the hour and hh:mm:ss after
- can you remove the execute tab and replace the current plan calendar with the current execute calendar (that shows the tracked and planned)
- now when the user want to time track the a task, they can do so with a button above the task name at the detailed view panel